"use client";
import React from "react";

const Button = () => {
  return (
    <div>
      <button
        className="bg-blue-500 text-white px-4 py-1 rounded-full"
        type="submit"
        onClick={() => {}}
      >
        Button
      </button>
    </div>
  );
};

export default Button;
